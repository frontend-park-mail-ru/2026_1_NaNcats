/**
 * Публичный барель универсального скелетона страницы.
 *
 * Экспортирует функциональный компонент {@link PageLoadingSkeleton}: его
 * использует Outlet как fallback Suspense, пока loader текущего роута ещё
 * не разрешился.
 */

export { PageLoadingSkeleton } from './PageLoadingSkeleton';
